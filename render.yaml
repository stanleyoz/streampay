services:
  - type: web
    name: streampay
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SKILL_BASE_URL
        sync: false